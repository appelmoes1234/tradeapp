# FTMO light versie

Gebruik CMD in deze map:

```cmd
npm.cmd install --omit=dev --ignore-scripts --no-audit --no-fund
npx.cmd next dev -H 127.0.0.1 -p 4000
```

Open daarna: http://127.0.0.1:4000

Deze light versie bevat geen Playwright dev dependency en geen node_modules/.next.
